@{
    var currentPath = Context.Request.Path.Value ?? "";

    if (User.Identity != null && User.Identity.IsAuthenticated && User.IsInRole("admin") && currentPath.StartsWith("/Admin", StringComparison.OrdinalIgnoreCase))
    {
        Layout = "_AdminLayout";
    }
    else
    {
        Layout = "_Layout";
    }
}